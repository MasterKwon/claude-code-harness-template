# Plugin: my-plugin

## 목적
이 플러그인은 프로젝트에서 공통으로 사용하는 설정 묶음입니다.
다른 팀/프로젝트에 배포할 때 이 폴더 전체를 복사하세요.

## 포함 내용
- 커스텀 슬래시 커맨드
- 공유 스킬
- 설정 템플릿

## 사용법
이 폴더를 대상 프로젝트의 `.claude/plugins/` 에 복사 후
`CLAUDE.md`에 아래 항목을 추가합니다:

```
## Plugins
- my-plugin: [플러그인 설명]
```

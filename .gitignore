# Claude Code 로컬 설정 (개인 환경마다 다름)
.claude/settings.local.json

# 실제 프로젝트 산출물 — 사용자 작업물은 git에 포함 (샘플은 포함)
# 추출 캐시만 제외
docs/00.input/extracted/

# Python
__pycache__/
*.pyc

# MCP 설정 (토큰 포함 가능 — 로컬에만 보관)
.mcp.json

# Node (실제 프로젝트 생성 후 생기는 것들)
node_modules/
.env
.env.local
